name: ci

on:
  push:
    branches: [main]

jobs:
  build:
    uses: lemrex/platform/.github/workflows/reusable-build.yml@v1.0.0
    with:
      service_name: test-orders
      language: node
    secrets:
      SWR_USERNAME: ${{ secrets.USERNAME }}
      SWR_PASSWORD: ${{ secrets.PASSWORD }}


      